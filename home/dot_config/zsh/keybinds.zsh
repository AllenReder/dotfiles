# Keybindings live here when they are not owned by a plugin.

# Keep the shell in the familiar emacs keymap. In this mode zsh-autosuggestions
# accepts the current suggestion through the forward-char widget.
bindkey -e
bindkey '^F' forward-char
